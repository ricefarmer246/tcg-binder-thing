import { useEffect, useState, type FormEvent } from 'react';
import { Link } from 'react-router-dom';
import { pokemonTypes, TypeDecal, type PokemonType } from '../components/TypeDecal';
type GuestCard = { id: string; name: string; type: PokemonType; hp: number };
const starterCards: GuestCard[] = [{ id: 'pikachu', name: 'Pikachu', type: 'Lightning', hp: 60 }, { id: 'charmander', name: 'Charmander', type: 'Fire', hp: 70 }, { id: 'squirtle', name: 'Squirtle', type: 'Water', hp: 60 }, { id: 'bulbasaur', name: 'Bulbasaur', type: 'Grass', hp: 70 }];
const STORAGE_KEY = 'tcg-binder-guest-cards';
function loadCards(): GuestCard[] { try { const saved = localStorage.getItem(STORAGE_KEY); return saved ? JSON.parse(saved) as GuestCard[] : starterCards; } catch { return starterCards; } }
export function GuestBinder() {
  const [cards, setCards] = useState<GuestCard[]>(loadCards); const [name, setName] = useState(''); const [type, setType] = useState<PokemonType>('Fire');
  useEffect(() => { localStorage.setItem(STORAGE_KEY, JSON.stringify(cards)); }, [cards]);
  function addCard(event: FormEvent) { event.preventDefault(); if (!name.trim()) return; setCards((current) => [...current, { id: crypto.randomUUID(), name: name.trim(), type, hp: Math.floor(Math.random() * 90) + 50 }]); setName(''); }
  return <main className="pokemon-page flex-1"><section className="mx-auto w-full max-w-6xl px-4 py-8 sm:py-12"><div className="guest-hero rounded-[2rem] p-6 sm:p-10"><div className="max-w-2xl"><p className="text-sm font-bold uppercase tracking-[0.22em] text-amber-800">Guest binder · saved on this device</p><h1 className="mt-3 text-4xl font-black tracking-tight text-slate-950 sm:text-5xl">Build your Pokémon card lineup.</h1><p className="mt-3 max-w-xl text-slate-700">No account needed. Add cards, choose their energy type, and keep this demo binder right in your browser.</p></div><div className="guest-decal-cluster" aria-hidden="true">{pokemonTypes.slice(0, 5).map((item) => <TypeDecal key={item.name} type={item.name} />)}</div></div><div className="mt-7 grid gap-6 lg:grid-cols-[19rem_1fr]"><aside className="rounded-3xl border border-amber-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-lg font-extrabold text-slate-900">Add a card</h2><span className="text-xs font-bold uppercase tracking-wider text-slate-400">Guest mode</span></div><form onSubmit={addCard} className="mt-4 space-y-4"><label className="block text-sm font-bold text-slate-700">Pokémon name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Gengar" className="mt-1.5 w-full rounded-xl border border-slate-300 px-3 py-2.5 outline-none focus:border-amber-500 focus:ring-2 focus:ring-amber-100" /></label><label className="block text-sm font-bold text-slate-700">Energy type<select value={type} onChange={(event) => setType(event.target.value as PokemonType)} className="mt-1.5 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 outline-none focus:border-amber-500">{pokemonTypes.map((item) => <option key={item.name}>{item.name}</option>)}</select></label><button className="w-full rounded-xl bg-slate-950 px-4 py-3 font-bold text-white transition hover:bg-slate-800">Add to binder</button></form><button onClick={() => setCards(starterCards)} className="mt-4 w-full text-sm font-medium text-slate-500 hover:text-slate-900">Reset demo cards</button></aside><section><div className="mb-4 flex items-end justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-amber-700">Pocket binder</p><h2 className="text-2xl font-black text-slate-950">Starter Collection <span className="text-base font-bold text-slate-400">{cards.length} cards</span></h2></div><Link to="/signup" className="hidden text-sm font-bold text-amber-700 hover:text-amber-900 sm:block">Want cloud sync? Sign up →</Link></div><div className="binder-page rounded-[1.5rem] p-4 sm:p-6"><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 xl:grid-cols-4">{cards.map((card) => <article key={card.id} className="pokemon-card"><div className="flex items-start justify-between gap-2"><h3 className="truncate text-sm font-black text-slate-950 sm:text-base">{card.name}</h3><span className="shrink-0 text-xs font-extrabold">{card.hp} HP</span></div><div className="pokemon-card-art" aria-hidden="true"><TypeDecal type={card.type} /></div><div className="flex items-center justify-between border-t border-black/10 pt-2 text-xs font-bold text-slate-600"><span>Basic</span><TypeDecal type={card.type} small /></div></article>)}</div></div></section></div></section></main>;
}
